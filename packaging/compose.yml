services:
  generate-artifacts:
    build:
      context: ..
      dockerfile: packaging/Dockerfile
    volumes:
      - ../out:/jcefout
    command: ["./packaging/scripts/generate_maven_builds.sh", "${BUILD_META_URL}", "${MVN_VERSION}"]
